{
    "name": "kev-dev",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
	    "test": "vitest",
		"test:ui": "vitest -u --ui --coverage.enabled=true --api 1337",
		"coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^2.0.5",
        "@vitest/ui": "^2.0.5",
        "sass": "^1.77.8",
        "typescript": "^5.5.3",
        "vite": "^5.4.0",
        "vitest": "^2.0.5"
    }
}
